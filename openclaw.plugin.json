{
  "id": "bmad-method",
  "name": "BMad Method",
  "description": "AI-driven agile development framework — the BMad Method as an OpenClaw plugin. Installs the official, always-current bmad-method skill catalog per-project and registers agent tools for skill dispatch and state tracking.",
  "version": "0.2.0",
  "kind": "tool",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "bmadVersion": {
        "type": "string",
        "description": "npm dist-tag or exact version of bmad-method to install for new projects (e.g. \"latest\", \"next\", \"6.11.0\"). Defaults to \"latest\"."
      }
    }
  },
  "uiHints": {
    "bmadVersion": {
      "label": "BMad-METHOD Version",
      "placeholder": "latest"
    }
  }
}
